import { BaseView } from './_base/BaseView.js';

export const AppView = (function() {

    // PRIVATE CODE BLOCK

    // PUBLIC CODE BLOCK
    class AppView extends BaseView {
        constructor(config) {
            super(config);
        }

    }

    return AppView;

})();
